# BlockPlan — freeze guard (SIH 2026, PS 26027)

This file exists so no tool, session, or teammate accidentally edits something
that must not change. Read it before touching anything under this tree.

The authoritative source of truth for project state, decisions, and rationale
is the PROJECT MEMORY document supplied at the start of the current
development phase. This file only enforces the freeze; it does not restate
the reasoning. If this file and the memory document disagree about a path or
a fact, the actual repository (below) wins, and the disagreement must be
raised explicitly, not silently resolved.

## Real paths (the memory document says `E:\SIH2026\...` — that is wrong)

    D:\SIH2026\Dataset\blockplan            the frozen analytical core
    D:\SIH2026\Dataset\blockplan-dataset    the frozen dataset + generation pipeline

## FROZEN — do not edit, refactor, reformat, or regenerate

- `D:\SIH2026\Dataset\blockplan\core.py` (44,044 bytes, 12 numbered sections).
  Every benchmark number anywhere in this project's documentation was produced
  by this exact file. Editing it invalidates all of them.
- Everything under `D:\SIH2026\Dataset\blockplan-dataset\dataset\` — raw
  sources, processed tables, scenario job sets, and the frozen benchmark
  artefacts (`method_comparison.csv`, `execution_scoring_summary.csv`,
  `benchmark_results.csv`).
- `D:\SIH2026\Dataset\blockplan\pairing_rules.csv` and
  `D:\SIH2026\Dataset\blockplan-dataset\dataset\processed\pairing_rules.csv` —
  the single source of truth for mandatory cross-department pairing rules.
- `D:\SIH2026\Dataset\blockplan-dataset\config\*.yaml` — frozen in practice
  even though not code.

**The only acceptable reason to change any of the above is a demonstrated
bug**, and that change must be described to the project owner, with the bug
shown, before it is made. "Cleaner," "more idiomatic," or "easier to wire into
the backend" are not acceptable reasons — threading parameters through
`core.py` instead of using module-level globals was already considered and
explicitly rejected. The agreed fix for the global-state problem is a
service-layer `threading.Lock`, not a rewrite of `core.py`.

## Folder roles

- `blockplan/` — the frozen analytical core. Imported by the backend, never
  edited by it.
- `blockplan-dataset/` — the frozen dataset and the pipeline that generated
  it. Read by the backend at startup, never regenerated by it.
- Everything else (backend, frontend, API contract) is product
  implementation, built around the frozen core, currently in progress.

## Rules for anyone (human or agent) working in this tree

1. Do not rewrite, refactor, reformat, or reorganise `core.py`.
2. Do not thread configuration parameters through `core.py` function
   signatures to avoid its module-level globals. Use a planning-service lock
   that resets state before each solve instead.
3. Do not regenerate, edit, or add columns to anything under
   `blockplan-dataset\dataset\`.
4. Reuse the existing loaders in
   `blockplan-dataset\src\blockplan_adapter.py` verbatim
   (`load_sections`, `load_trains`, `load_jobs`, `load_scenario_jobs`,
   `load_scenarios`, `load_config_into_core`, `load_pairing_rules_into_core`,
   `add_peak_passenger_traffic`, `remove_disrupted_windows`,
   `apply_freight_scenario`). Do not rewrite them.
5. Never describe the maintenance jobs, block requests, freight paths, or
   execution realisations as real Indian Railways operational data. They are
   synthetic, generated from documented distributions and rules, clearly
   labelled as such in `dataset\metadata\manifest.json`. Infrastructure,
   station data, and the real train timetable are genuinely real public data
   (DataMeet / Indian Railways, CC0) — that distinction must never be blurred
   in either direction.
6. No ML, no LLM, no chatbot, no database, no Redis/Celery/RQ, no
   WebSockets/SSE, no microservices — none of these have a concrete
   requirement behind them for this project. Do not add any of them without
   first stating the requirement that needs it.
7. Follow the phased roadmap (Phase 0 → 9) already agreed for this project.
   Do not substitute a generic software development plan.

## Known issue: B4/OURS RNG reset ordering in blockplan_adapter.py

**Corrected 2026-09-06. Read this before re-running any benchmark.**

`run_method_comparison()` in
`blockplan-dataset\src\blockplan_adapter.py` resets the Monte Carlo RNG to a
canonical saved state before each baseline, so every method's reliability
draws start from the same place and the six methods stay comparable. B0–B3
did this correctly. **B4 and OURS did not**: their `reset_mc_rng()` call sat
*after* `core.build_columns()` and immediately before `core.solve()`, where it
does nothing — `build_columns()` is the RNG-consuming step (it runs
`reliability_mc`), while `solve()` never touches `core.RNG` at all (CP-SAT
uses its own `random_seed`). B4 was therefore sampling from whatever RNG state
B3 happened to leave behind.

The correction moves `reset_mc_rng()` to immediately before `build_columns()`
for both B4 and OURS, matching the B0–B3 pattern.

- **The fix is in the adapter, not `core.py`.** `core.py` was not touched and
  remains byte-identical (44,044 bytes).
- **Measured effect (isolated verification, NORMAL_TRAFFIC):** with the
  ordering corrected, B4 yields `traffic_cost = 259.8` and
  `exp_overrun_cost = 79.9` — matching the project memory document's §22.1 B4
  row exactly. With the old ordering it yielded 272.8 / 69.0. B0, B1, B2, B3
  and OURS are unaffected.
- **The existing benchmark CSVs remain the authoritative frozen artifacts.**
  `dataset\scenarios\method_comparison.csv` and
  `dataset\scenarios\benchmark_results.csv` were NOT regenerated, and the
  corrected-ordering figures are NOT a new baseline. `method_comparison.csv`
  still carries the older B4 row (138 / 272.8 / 69.0). Reconciling that row
  against the memory document is a separate, unmade decision.
- Anyone re-running the benchmark should expect B4 to differ from the
  checked-in CSV for this reason, and must not overwrite these files without
  an explicit decision to re-baseline.

### Reproducibility caveat (separate from the above)

There is no dependency lockfile in this project. Verified working versions as
of 2026-09-06: `numpy 2.5.2`, `ortools 9.15.6755`, `PyYAML 6.0.3`, Python
3.14.6. The versions that produced the frozen artifacts were never recorded.

Independently of the RNG ordering, this instance is highly degenerate — many
distinct plans share exactly the same optimal objective — so tiny
floating-point differences between library versions can flip CP-SAT's choice
among tied optima. Observed effect is about one block: block count can move by
one, and which specific block is cross-department can change, while objective,
traffic cost and reliability hold. Quote objective/traffic/reliability figures
with confidence; do not claim bit-identical cross-department block attribution
across machines.

## Where things are right now

No backend, no frontend, no database, no API implementation exists yet. Do
not assume any of them do. Current phase: Phase 0 (freeze guard + API
contract). See `API_CONTRACT.md` and `plan.json` at this same root for the
other two Phase 0 deliverables.
