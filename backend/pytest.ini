[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --durations=10
markers =
    slow: runs a real solve in a subprocess (minutes). Deselect with -m "not slow".
filterwarnings =
    error::DeprecationWarning:blockplan_service.*
